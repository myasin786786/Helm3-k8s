apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nodeapp-charts.fullname" . }}-test-connection"
  labels:
    {{- include "nodeapp-charts.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nodeapp-charts.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
