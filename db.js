...
const {
  root,
  root,
  192.168.88.175,192.168.88.127,
  27017,
  ReplicationTestDB,
  rs0
} = process.env;

...
const url = `mongodb://${MONGO_USERNAME}:${MONGO_PASSWORD}@${MONGO_HOSTNAME}:${MONGO_PORT}/${MONGO_DB}?replicaSet=${MONGO_REPLICASET}&authSource=admin`;
...
